# Text To SQL

Мультиагентную систему на langgraph, которая зайдествует SQL-интерпретатор. Она на вход получает структурированные данные о новом рекламном предложении в виде json. Далее она с помощью LLM в текстовом виде генерирует критерии для сегмента целевой аудитории. После этого она по этим критериям генерирует SQL-скрипт для извлечения айдишников пользователей, которые удовлетворяют этим критериям. На выход система отдает критерии в текстовом виде, SQL-скрипт и результат SQL скрипта.

Проект использует несколько специализированных «агентов» (модулей), которые последовательно:
- интерпретируют входные данные,
- строят SQL-запрос к целевой БД,
- при необходимости валидируют и исправляют результат.

---

## Возможности

- ✅ **Валидация на датасете**: отдельный скрипт для оценки качества на валидационном датасете (`tests/test.py`).
- 🧹 **Автоматический линтинг и форматирование**:
  - GitHub Actions workflow с запуском Ruff на каждый push / pull request.
  - Локальный `pre-commit` с Ruff (линтер + форматтер).

---

## Структура репозитория

Примерная структура (ключевые элементы):

```text
.
├── src/
│   ├── main.py          # Точка входа в систему Text To SQL
│   └── ...              # Остальной код агентов и инфраструктуры
├── tests/
│   └── test.py          # Скрипт валидации системы на валидационном датасете
├── .pre-commit-config.yml
├── .github/
│   └── workflows/
│       └── ruff_lint.yml
└── README.md
```