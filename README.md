# AdBanner GenAI

Система автоматической генерации рекламных креативов, объединяющая LLM и диффузионные модели. На вход она получает параметры рекламируемого продукта, текст рекламного оффера, технический формат баннера и опциально текстовые описания изображений для генерации на баннере. Далее она с помощью LLM декомпозирует оффер на текстовые блоки и адаптирует их под ограничения формата. Визуальная часть формируется двумя путями: либо через семантический поиск подходящего изображения в векторной базе, либо через генерацию нового изображения с сохранением корпоративного стиля. На выход система отдает готовый, сверстанный графический файл (PNG/JPG).

Проект использует сложный пайплайн обработки данных, который включает:
- Генерацию и итеративное сокращение текстов под лимиты формата (LLM + Retry logic),
- Генерацию изображений (Qwen-Image + LoRA) или их поиск по смыслу (Text Encoder + Qdrant),
- Постпроцессинг (удаление фона через BiRef-Net) и финальную сборку макета.

---

## Возможности

- 🧹 **Автоматический линтинг и форматирование**:
  - GitHub Actions workflow с запуском Ruff на каждый push / pull request.
  - Локальный `pre-commit` с Ruff (линтер + форматтер).

---

## Структура репозитория

Примерная структура (ключевые элементы):

```text
.
├── src/
│   └── eda   
|     └── ... # Файлы eda         
├── 
├── .pre-commit-config.yaml
├── .github/
│   └── workflows/
│       └── ruff_lint.yml
└── README.md
```